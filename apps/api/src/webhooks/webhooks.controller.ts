/* eslint-disable @typescript-eslint/consistent-type-imports */
import { Body, Controller, Delete, Get, Param, Patch, Post, Req, UseGuards } from '@nestjs/common';
import { JwtAuthGuard } from '../auth/jwt-auth.guard';
import type { CreateWebhookDto, UpdateWebhookDto } from './dto';
import { WebhooksService } from './webhooks.service';
import { WebhookEventsService } from './webhook-events.service';
/* eslint-enable @typescript-eslint/consistent-type-imports */
@Controller('workspaces/:workspaceId/webhooks') @UseGuards(JwtAuthGuard) export class WebhooksController { constructor(private readonly service:WebhooksService, private readonly events:WebhookEventsService){} @Get() list(@Req()r:{user:{sub:string}},@Param('workspaceId')id:string){return this.service.list(r.user.sub,id).then((data)=>({success:true,message:'Webhooks loaded',data}));} @Post() create(@Req()r:{user:{sub:string}},@Param('workspaceId')id:string,@Body()dto:CreateWebhookDto){return this.service.create(r.user.sub,id,dto).then((data)=>({success:true,message:'Webhook created. Store the signing secret now; it will not be shown again.',data}));} @Patch(':webhookId') update(@Req()r:{user:{sub:string}},@Param('workspaceId')id:string,@Param('webhookId')hookId:string,@Body()dto:UpdateWebhookDto){return this.service.update(r.user.sub,id,hookId,dto).then((data)=>({success:true,message:'Webhook updated',data}));} @Post(':webhookId/test') test(@Req()r:{user:{sub:string}},@Param('workspaceId')id:string,@Param('webhookId')hookId:string){return this.service.authorizeManager(r.user.sub,id).then(()=>this.events.test(r.user.sub,id,hookId)).then((data)=>({success:true,message:'Webhook test queued',data}));} @Get(':webhookId/deliveries') deliveries(@Req()r:{user:{sub:string}},@Param('workspaceId')id:string,@Param('webhookId')hookId:string){return this.service.authorizeManager(r.user.sub,id).then(()=>this.events.listDeliveries(id,hookId)).then((data)=>({success:true,message:'Webhook deliveries loaded',data}));} @Post(':webhookId/deliveries/:deliveryId/retry') retry(@Req()r:{user:{sub:string}},@Param('workspaceId')id:string,@Param('webhookId')hookId:string,@Param('deliveryId')deliveryId:string){return this.service.authorizeManager(r.user.sub,id).then(()=>this.events.retryDelivery(id,hookId,deliveryId)).then((data)=>({success:true,message:'Webhook delivery queued',data}));} @Delete(':webhookId') remove(@Req()r:{user:{sub:string}},@Param('workspaceId')id:string,@Param('webhookId')hookId:string){return this.service.remove(r.user.sub,id,hookId).then(()=>({success:true,message:'Webhook deleted',data:null}));} }
