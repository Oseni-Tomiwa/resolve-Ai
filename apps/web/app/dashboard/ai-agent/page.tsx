import { EmptyState } from '../empty-state';
export default function AiAgent() { return <EmptyState eyebrow="AI Agent" title="Your support voice starts here." description="Tone, escalation rules, and model configuration will be available when the ResolveAI agent is ready to be configured." />; }
