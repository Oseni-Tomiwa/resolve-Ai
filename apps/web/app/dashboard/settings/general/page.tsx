import { EmptyState } from '../../empty-state';
export default function GeneralSettings() { return <EmptyState eyebrow="General settings" title="Workspace details are coming next." description="Organization and workspace names are already connected to your account. Editing controls will arrive with the settings experience." />; }
