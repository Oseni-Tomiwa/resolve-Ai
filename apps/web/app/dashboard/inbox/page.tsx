import { EmptyState } from '../empty-state';
export default function Inbox() { return <EmptyState eyebrow="Inbox" title="Your support inbox is waiting." description="Customer conversations will appear here once a support channel is connected. This space will keep the team focused on what needs a response." />; }
