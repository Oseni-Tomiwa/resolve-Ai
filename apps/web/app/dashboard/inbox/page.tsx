'use client';

import { useCallback, useEffect, useState, type FormEvent } from 'react';
import { useDashboard } from '../dashboard-context';
import { apiRequest } from '../../api-client';

type Item = { id: string; title: string; status: string; mode: string; priority: string; unread: boolean; lastMessage?: { role: string; content: string } | null; assignedUser?: { firstName: string; lastName: string } | null };
type Detail = Item & { messages: Array<{ id: string; role: string; content: string; createdAt: string }>; notes: Array<{ id: string; content: string; author: { firstName: string; lastName: string } }> };
type List = { items: Item[]; unreadCount: number; canManage: boolean };

export default function InboxPage() {
  const { currentWorkspace } = useDashboard();
  const [list, setList] = useState<List | null>(null); const [selected, setSelected] = useState<Detail | null>(null); const [status, setStatus] = useState(''); const [error, setError] = useState(''); const [reply, setReply] = useState(''); const [note, setNote] = useState(''); const [busy, setBusy] = useState(false);
  const load = useCallback(async () => { if (!currentWorkspace) return; try { const value = await apiRequest<List>(`/workspaces/${currentWorkspace.id}/inbox${status ? `?status=${status}` : ''}`); setList(value); if (selected) setSelected(await apiRequest<Detail>(`/workspaces/${currentWorkspace.id}/inbox/${selected.id}`)); } catch (caught) { setError(caught instanceof Error ? caught.message : 'Unable to load the inbox.'); } }, [currentWorkspace, selected, status]);
  useEffect(() => { void load(); }, [load]);
  useEffect(() => { if (typeof document === 'undefined') return; const timer = window.setInterval(() => { if (!document.hidden) void load(); }, 3000); return () => window.clearInterval(timer); }, [load]);
  async function select(id: string): Promise<void> { if (!currentWorkspace) return; const value = await apiRequest<Detail>(`/workspaces/${currentWorkspace.id}/inbox/${id}`); setSelected(value); await apiRequest(`/workspaces/${currentWorkspace.id}/inbox/${id}/read`, { method: 'POST' }); void load(); }
  async function action(path: string, init?: RequestInit): Promise<void> { if (!currentWorkspace || !selected) return; setBusy(true); try { await apiRequest(`/workspaces/${currentWorkspace.id}/inbox/${selected.id}${path}`, init); await load(); } catch (caught) { setError(caught instanceof Error ? caught.message : 'Inbox action failed.'); } finally { setBusy(false); } }
  async function submit(event: FormEvent, kind: 'reply' | 'note'): Promise<void> { event.preventDefault(); const content = kind === 'reply' ? reply : note; if (!content.trim()) return; await action(kind === 'reply' ? '/messages' : '/notes', { method: 'POST', body: JSON.stringify({ content }) }); if (kind === 'reply') setReply(''); else setNote(''); }
  return <section className="team-page"><div className="knowledge-intro"><div><p className="eyebrow">Customer support</p><h2>Inbox</h2><p>Take over visitor conversations when a human should step in.</p></div><span className="section-hint">{list?.unreadCount ?? 0} unread</span></div>{error && <div className="dashboard-error" role="alert">{error}</div>}<div className="inbox-layout"><aside className="inbox-list"><div className="knowledge-filters"><label className="visually-hidden" htmlFor="inbox-status">Filter status</label><select id="inbox-status" value={status} onChange={(event) => setStatus(event.target.value)}><option value="">All</option><option value="OPEN">Open</option><option value="PENDING">Pending</option><option value="RESOLVED">Resolved</option></select></div>{list?.items.map((item) => <button className={`inbox-row${selected?.id === item.id ? ' active' : ''}`} key={item.id} type="button" onClick={() => void select(item.id)}><strong>{item.title}</strong><small>{item.lastMessage?.content || 'No messages yet'}</small><span>{item.status} · {item.mode}{item.unread ? ' · Unread' : ''}</span></button>)}</aside><main className="inbox-detail">{!selected ? <div className="knowledge-empty"><h3>Select a conversation</h3><p>Visitor messages will appear here.</p></div> : <><div className="section-heading"><div><p className="eyebrow">{selected.mode} · {selected.priority}</p><h3>{selected.title}</h3></div><div className="knowledge-search-row"><button className="button button-small" disabled={busy || selected.mode === 'HUMAN'} onClick={() => void action('/takeover')}>Take over</button><button className="text-action" disabled={busy || selected.mode === 'AI'} onClick={() => void action('/return-to-ai')}>Return to AI</button><select value={selected.status} onChange={(event) => void action('/status', { method: 'PATCH', body: JSON.stringify({ status: event.target.value }) })}><option value="OPEN">Open</option><option value="PENDING">Pending</option><option value="RESOLVED">Resolved</option></select></div></div><div className="inbox-messages">{selected.messages.map((message) => <article className={`inbox-message ${message.role.toLowerCase()}`} key={message.id}><small>{message.role}</small><p>{message.content}</p></article>)}</div><form className="knowledge-search-row" onSubmit={(event) => void submit(event, 'reply')}><input aria-label="Reply to visitor" value={reply} onChange={(event) => setReply(event.target.value)} placeholder="Reply to visitor…" /><button className="button button-small" disabled={busy || selected.mode !== 'HUMAN'}>Send reply</button></form><form className="knowledge-search-row" onSubmit={(event) => void submit(event, 'note')}><input aria-label="Internal note" value={note} onChange={(event) => setNote(event.target.value)} placeholder="Add internal note…" /><button className="text-action" disabled={busy}>Add note</button></form>{selected.notes.length > 0 && <div className="inbox-notes"><p className="eyebrow">Internal notes</p>{selected.notes.map((item) => <p key={item.id}><strong>{item.author.firstName} {item.author.lastName}</strong>: {item.content}</p>)}</div>}</>}</main></div></section>;
}
